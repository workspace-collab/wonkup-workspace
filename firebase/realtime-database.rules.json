{
  "rules": {
    "presence": {
      "$workspaceId": {
        "$projectId": {
          "$userId": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid === $userId"
          }
        }
      }
    }
  }
}
